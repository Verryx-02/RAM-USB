# Known Issues (cache)

This file is a scratch cache, not the durable record. GitHub Issues
(label [`known-issue`](https://github.com/Verryx-02/RAM-USB/issues?q=is%3Aissue+label%3Aknown-issue),
using `.github/ISSUE_TEMPLATE/known_issue.yml`) is the durable tracker —
distinct from the SRS's own section 8 ("Known risks and open issues"), which is
for deliberately-deferred requirement-level decisions requiring the
SRS's own approval process.

**Workflow:** when any agent (orchestrator or subagent) finds an
implementation gap or bug outside its current task's scope, it notes it
below immediately, instead of letting it evaporate at the end of that
task's report. The user then reviews the note, reasons about whether
it's worth tracking, and decides whether to open it as a real GitHub
issue. No agent may create, close, edit, or comment on a GitHub issue
itself (`.claude/settings.json` denies it, same enforcement as the git
non-negotiables) — at most, prepare the exact `gh issue create ...`
command for the user to run themselves. Once a finding is promoted to a
GitHub issue, or the user decides it isn't worth one, remove it from
below — this file should stay close to empty between sessions, not
accumulate history.

**Migrated 2026-07-28:** the 30 entries this file used to hold (KI-01
through KI-31, skipping KI-08) are now GitHub issues under the
`known-issue` label.

**Migrated 2026-08-03:** KI-32 through KI-89 — the findings from the full
Go code review, the deployment-layer audit, and the merge and verification
work that followed — are now GitHub issues under the same label. Of those,
KI-32..KI-44, KI-47, KI-49, KI-56..KI-60, KI-66..KI-71, KI-78, KI-81..KI-83
were fixed and merged the same day; the rest remain open. Two are recorded
as accepted rather than fixed, by explicit decision: KI-87 (a residual
grant/sweep race) and KI-88 (the hashed-email migration dropping existing
rows, acceptable while there are no real users).

---

## Findings from the ponytail over-engineering audit (2026-08-04)

Read-only scan across `pkg/`, all 7 services, `user-client/`, and
`deployments/` for over-engineering only (dead code, unused abstractions,
hand-rolled stdlib, duplicated boilerplate) — correctness/security/
performance were out of scope. Two other findings from the same pass
already matched existing GitHub issues and were not re-added here: the
dead dialer-injection plumbing duplicates KI-51 (issue #50), and
`RequestCounters.Track` being half-adopted duplicates KI-53's point 2
(issue #52). While cross-checking, also found that `tailscale.com` in
`go.mod` is confirmed dead (`go mod why tailscale.com` → "main module
does not need package tailscale.com") — already tracked as KI-54 point 3
(issue #53), no new entry needed.

| # | Finding | Location |
|---|---|---|
| KI-90 | Tailscale mesh-sidecar block (`image`/`cap_add`/`devices`/`volumes`/`TS_USERSPACE`/entrypoint) copy-pasted verbatim across 4 compose files. A shared fragment via Compose `include:` + YAML merge key would collapse it to one, varying only hostname/authkey. | `deployments/compose/{certificate-authority,mqtt-broker,grafana,entry-hub}.yml` |
| KI-91 | step-cli cert-issuer/renewer sidecar pair (cp root cert, `step ca certificate --token`, chown/chmod, `step ca renew --daemon --exec`) copy-pasted per third-party binary across 3 compose files. One parameterized script (uid/SAN/cert-basename as args) would replace the inlined heredoc per file. | `deployments/compose/{mqtt-broker,metrics-collector,grafana}.yml` |
| KI-92 | Byte-identical 3-line `networks:\n  ramusb-net:\n    external: true` trailer repeated in all 11 compose files. One shared fragment via Compose `include:`. | `deployments/compose/*.yml` |
| KI-93 | 3-line `step ca token ... --provisioner admin` bootstrap-token mint call duplicated verbatim (name substituted only) across 9 shell scripts. One sourced `mint_ca_token()` helper. Distinct from KI-77, which is about `set -e` being defeated in the same scripts, not this duplication. | `deployments/scripts/*.sh` |
| KI-94 | 3-line Headscale user-create + preauthkey-mint sequence duplicated verbatim across 7 shell scripts. One sourced `mint_mesh_authkey()` helper. | `deployments/scripts/*.sh` |
| KI-95 | network-manager's `pushStartupPolicy` — a 1-line forward to `headscale.PushPolicy` existing only to justify a 79-line duplicate test file (`main_test.go`) of `policy_test.go`'s own coverage. Call `headscale.PushPolicy` directly in `run()`, delete the duplicate test file. | `services/network-manager/cmd/network-manager/main.go:532` |
| KI-96 | `PolicyPusher.GetPolicy`/`Client.GetPolicy` — implemented and interface-declared, never called by production code, only exercised by test round-trips. `PolicyPusher` shrinks to `SetPolicy` alone. | `services/network-manager/internal/headscale/policy.go:371-374`, `rest.go:298-309` |
| KI-97 | metrics-collector's `store.PoolQuerier` — the type's own doc comment admits a bare `*pgxpool.Pool` already satisfies `Querier` structurally. Pass `store.Store{DB: pool}` directly. | `services/metrics-collector/internal/store/store.go:40-55` |
| KI-98 | user-client `execrunner.Fake`'s `OutputFor`/`ErrFor` per-call override maps — no real caller uses them (`main_test.go` notes keying by full command string is impractical). Plain `Output`/`Err` fields suffice. | `user-client/internal/execrunner/fake.go:18-27,39-48` |
| KI-99 | storage-service's one-line `parseID` wrapper (`return strconv.Atoi(s)`) inlineable at both `Chown` call sites; network-manager's `unionTag` hand-rolled "already present" loop replaceable with `slices.Contains`. | `services/storage-service/internal/posixuser/dirmaker.go:73-92`, `services/network-manager/internal/headscale/client.go:329-336` |
| KI-100 | `make diagrams` (Makefile) regenerates **every** `.puml` in `docs/design/diagrams/` in one shot (`find /data -name "*.puml" ...`), not just the file a task is editing. Diagrams 01 and 09 both depend on hand-drawn Python post-processing scripts (`*-01.py`) layered on top of the raw PlantUML render (arrows, widened boxes, labels not expressible in `.puml`/`smetana`/`dot`). Any task that touches one diagram and runs `make diagrams` silently wipes every *other* diagram's post-processing back to bare PlantUML output unless that task also reruns the other diagram's full script chain — reproduced live 2026-08-06: a diagram-09 task's `make diagrams` call reset diagram 01's SVG (missing arrows, box back to unwidened) minutes after it had just been fixed. Fix would be either a per-file Makefile target, or a documented rule that every diagram-agent task must rerun *all* diagrams' post-processing scripts (not just the one it's working on) whenever it invokes `make diagrams`. | `Makefile:1-2`, `docs/design/diagrams/*-01.py` |

## Findings from a tech-stack survey for the thesis's "Tecnologie utilizzate" chapter (2026-08-07)

Read-only exploration of `go.mod`, every service's Dockerfile, and `deployments/compose/*.yml`, done to ground a thesis chapter in the real stack rather than SRS prose. Surfaced drift between the SRS and the actual code, out of scope for the thesis-writing task itself.

| # | Finding | Location |
|---|---|---|
| KI-101 | SRS §2.4's note states distroless is "the default" container base image, with `debian:bookworm-slim` as the exception. In the real Dockerfiles, it's the reverse: only Entry-Hub actually uses `gcr.io/distroless/static-debian12:nonroot`; Security-Switch, Storage-Service, Network-Manager, Database-Vault, and Metrics-Collector all use `debian:bookworm-slim` (each Dockerfile has its own comment justifying it — needing a real OS-level `tailscaled` rather than in-process `tsnet`, `sshd`, POSIX user creation, etc.). The SRS's "default" framing is stale. | `docs/Software_Requirements_Specification.md:106` vs `deployments/docker/{entry-hub,security-switch,storage-service,network-manager,database-vault,metrics-collector}/Dockerfile` |
| KI-102 | SRS §2.1 states RAM-USB is "11 Docker containers." The real topology has consolidated past that: Database-Vault's image bundles its own `postgres:17`; Metrics-Collector's image bundles `timescale/timescaledb:2.23.0-pg18`; Certificate-Authority's image bundles the metrics-sidecar binary inside `smallstep/step-ca`. The component table (§2.1) doesn't reflect this consolidation. | `docs/Software_Requirements_Specification.md:66-80` vs `deployments/docker/{database-vault,metrics-collector,certificate-authority}/Dockerfile` |
| KI-103 | Two container image tags are unpinned in compose files: `smallstep/step-ca:latest` (Certificate-Authority's runtime base) and `tailscale/tailscale:latest` (used across several mesh-sidecar containers). Unlike the Go module deps (all pinned) and the OS-level `tailscaled` baked into some images (pinned `1.98.9`), these two give no reproducible version at the container level. | `deployments/compose/certificate-authority.yml:169`, mesh-sidecar blocks in `deployments/compose/{entry-hub,grafana,mqtt-broker,certificate-authority}.yml` |
| KI-104 | Whether production Entry-Hub actually automates Let's Encrypt issuance (EH-F-01/02/03) is unverified from the Go code: no ACME client import exists anywhere in the repo (no `certmagic`/`lego`/similar), the only cert-provisioning code found is a dev-only self-signed-cert script. If production issuance is a manual/external step, the SRS doesn't say so explicitly. | `third-party/entry-hub/generate-dev-cert.sh`; SRS EH-F-01/02/03 |

## Finding from embedding a diagram in the thesis (2026-08-09)

| # | Finding | Location |
|---|---|---|
| KI-105 | `ThesisAtUniud/figures/*.pdf` (currently 4 files: `00-usecases-overview`, `01-architecture-container`, `08-security-pki-hierarchy`, `09-security-trust-zones`) are manually-exported PDF copies of the matching `docs/design/diagrams/*.puml`, needed because the thesis is compiled with `pdflatex`, which cannot `\includegraphics` an `.svg` directly, while `make diagrams` (Makefile) only renders `-tsvg`. No Makefile target produces these PDFs, and neither `CONTRIBUTING.md` §6 nor any thesis file documents the export step. If a `.puml` source changes later and `make diagrams` regenerates its SVG, the matching thesis PDF will silently go stale — same failure shape as KI-100, but for the thesis's copies instead of a second diagram's post-processing. **Also found while investigating a "why aren't the icons vector" question (2026-08-09):** the plain conversion command, `rsvg-convert -f pdf -o out.pdf in.svg`, is not just a stale-copy risk but produces visibly low-resolution output on its own, every run — every node in these diagrams carries `_style.puml`'s drop-shadow filter (`feGaussianBlur`), and `rsvg-convert` rasterizes any filtered element at a fixed 72 DPI when targeting PDF instead of preserving it as vector (confirmed with `pdfimages -list`: plain export → 755×651px tiles at 72 ppi; `--zoom 6` → 4527×3906px tiles, ~432 ppi at the same print size, confirmed by recompiling and re-inspecting). The 4 files above have been regenerated with `--zoom 6` as a workaround; the same flag must be used for any future manual export of this kind, or the low-resolution regression comes back silently. Fix would be adding a `-tpdf` render step to PlantUML's own invocation (either to the same Makefile target or a dedicated one), which may avoid the rasterization issue at the source instead of needing the `--zoom` workaround downstream — untested, blocked on Docker not being available in this session to verify. | `Makefile:1-2`, `ThesisAtUniud/figures/*.pdf`, `docs/design/diagrams/{00-usecases-overview,01-architecture-container,08-security-pki-hierarchy,09-security-trust-zones}.puml`, `docs/design/diagrams/_style.puml` |

## Finding from analyzing thesis chapter 4 against the SRS (2026-08-13)

| # | Finding | Location |
|---|---|---|
| KI-108 | `deployments/scripts/cleanup.sh`'s container removal loop (line 23-26) omits `entry-hub-mesh` (Entry-Hub's Tailscale sidecar, added KI-27) and every project-prefixed transient container Compose creates on first `up` for a service (e.g. `ramusb-certificate-authority-certificate-authority-{init,config-init}-1`, `ramusb-mqtt-broker-mqtt-broker-{certs-init,cert-issuer}-1`, `ramusb-metrics-collector-metrics-collector-timescaledb-cert-issuer-1`) and the plain-named `*-cert-renewer` containers (`mqtt-broker-cert-renewer`, `metrics-collector-timescaledb-cert-renewer`). Confirmed live 2026-08-14: after `cleanup.sh --wipe`, `docker ps -a` still showed 8 leftover containers including a live, running `entry-hub-mesh`, requiring a manual `docker rm -f` pass to get a genuinely clean slate. Fix would be extending the loop's container list (or switching to a label-based `docker rm -f $(docker ps -aq --filter label=com.docker.compose.project=ramusb-*)` sweep) so a single `cleanup.sh --wipe` run is actually complete. | `deployments/scripts/cleanup.sh:23-28` |
| KI-107 | `deployments/scripts/e2e-test.sh`'s login step (`curl ... https://localhost:8443/api/login`) can never succeed as written, independent of the EH-F-01/EH-F-02 rename fixed the same session: EH-F-03's login listener binds to the mesh IP only (`RAM_USB_ENTRY_HUB_LOGIN_LISTEN_ADDR=":8446"`, by design), and only port 8443 is published to the host (`deployments/compose/entry-hub.yml` `ports:`). A curl run from the host machine against `localhost:8443/api/login` returns 404 (route not registered on that listener) even with a valid `pre_auth_key` from a fully mesh-joined test client. Confirmed live 2026-08-14 running the full 9-shell dev stack end to end. Fix would be running the login curl from inside the already-mesh-joined `ramusb-tailscale-test` container (`docker exec ramusb-tailscale-test curl ...`) against Entry-Hub's mesh IP:8446, not from the host. | `deployments/scripts/e2e-test.sh:40-44`, `deployments/compose/entry-hub.yml:57,75-76` |
| KI-112 | The email lookup key is an unkeyed SHA-256 (`hashing.HashEmail`: lowercase, then a bare `sha256.Sum256`), while the pepper (`RAM_USB_PASSWORD_PEPPER`) is used only in the password path. Anyone holding a copy of the database can therefore test whether a candidate address is registered, at SHA-256 speed, over the small and guessable space of plausible email addresses. A pepper would close this without breaking anything: unlike a per-record salt, a single global secret keeps the digest deterministic, so DV-F-13's login lookup still recomputes the same key. The correct construction is a keyed hash (HMAC-SHA256 under the pepper), not the naive concatenation used in the password path, which is safe there only because Argon2id follows it. This is a different threat from RISK-05: that one is an online oracle against the API, this one is offline against a stolen dump. Costs: it is an SRS-level change to DV-F-03/DV-F-13; every stored `email_hash` becomes invalid, though rebuildable by decrypting the email with the master key (it is encrypted, not only hashed, so the change is reversible); and the pepper becomes load-bearing for lookups, not just for password verification, so losing it would mean losing the ability to find records at all, which belongs next to RISK-02. Recorded in the thesis's future-work section (chapter 12). Surfaced 2026-08-18 while reviewing chapter 5's credential-handling prose. | `services/database-vault/internal/hashing/hashing.go`, `services/database-vault/internal/password/pepper.go`, SRS DV-F-03/DV-F-06/DV-F-13, RISK-02 |
| KI-111 | The Client never maps HTTP 409 to a meaningful error. `mapStatusError` switches on 400, 401, 403, 502, 503 and 504, and everything else falls through to `default` → `apperrors.NewInternal`, so a duplicate registration (DV-F-12's explicit outcome, and UC-01's first alternative flow) is reported to the user as an internal server failure instead of "email or SSH key already registered". This is the single most likely failure a real user hits on registration. The code matches its requirement: CL-F-08 lists only "400/401/403/500/502/503/504", omitting 409 even though DV-F-12 and UC-01 both specify it and Entry-Hub relays it byte-for-byte through `writeForwardedResponse`. Fix is one `case http.StatusConflict` plus the matching CL-F-08 wording in the SRS. Surfaced 2026-08-18 while tabulating the HTTP status codes for thesis chapter 5. | `user-client/internal/entryhub/client.go` (`mapStatusError`), SRS CL-F-08 vs DV-F-12/UC-01 |
| KI-110 | The Client persists two things the SRS never specifies, and both packages say so themselves in a "DESIGN DECISION FLAGGED FOR REVIEW" header comment. (1) `internal/reposecret` sources restic's own repository-encryption password, generating a random 32-byte value on first use and storing it at `os.UserConfigDir()/ram-usb/restic-password` (mode 0600). This is the secret that actually delivers RNF-SEC-01's zero-knowledge guarantee for backup content, and no CL-F requirement covers it: CL-F-06/CL-F-07 only specify restic's *transport* authentication (the SSH key from CL-F-01), not restic's separate repository secret. Losing that file means the user's existing snapshots cannot be decrypted, a per-user analogue of RISK-02's master-key exposure, with no backup procedure specified either. (2) `internal/clientstate` persists the POSIX username Storage-Service assigned at registration (mode 0644), because CL-F-02 and CL-F-06/07 are each independently "following a user command" and UC-01/UC-03 place register and backup arbitrarily far apart in time, but no requirement says the Client may keep state between invocations. Fix would be adding two CL-F requirements (repository-secret generation/storage/loss-consequences, and permitted local client state) rather than leaving both as implementation judgment calls. Surfaced 2026-08-17 while scoping a thesis chapter 4 subsection about the Client. | `user-client/internal/reposecret/reposecret.go:1-21`, `user-client/internal/clientstate/clientstate.go:1-19`, SRS CL-F-01/CL-F-06/CL-F-07, RNF-SEC-01 |
| KI-109 | SRS EH-F-04's requirement text still names the pre-rename path: "`/api/register` must accept JSON and validate: ...". The rename to `POST /api/users` (EH-F-02, landed in f105c0f) propagated to EH-F-02, CL-F-02, CL-F-09 and UC-01, but not into EH-F-04's own body, which is now the only place in the SRS still asserting `/api/register` outside the version-history note in the header. EH-F-05 is unaffected (`/api/login` is deliberately unchanged). The thesis appendix reproducing these tables already says `/api/users`, so the SRS is currently the less accurate of the two. Surfaced while reading the SRS against thesis chapters 3-4 (2026-08-17). | `docs/Software_Requirements_Specification.md:168` vs `services/entry-hub/internal/httpapi/handler.go:44`, `ThesisAtUniud/appendices/a_appendice.tex:58` |
| KI-106 | `POST /api/register` (public, EH-F-02) lets an unauthenticated caller determine whether a given email is already registered, and this is not an implementation slip — it is the literal, deliberate behavior of DV-F-12/`NewConflict`. Submitting a candidate email + valid-format password + a freshly generated (attacker-controlled, always-unique) SSH public key returns 201 if the email was unregistered or 409 if it already existed; `NewConflict`'s own doc comment only hides *which* field collided (email vs SSH key), not *that* one did, and the status code is relayed byte-for-byte end-to-end (Database-Vault → Security-Switch → Entry-Hub's `writeForwardedResponse`, confirmed in code). Contrast with DV-F-15/`NewUnauthorized` (login), which deliberately collapses "no such account" and "wrong password" into the same 401 specifically to prevent this exact class of enumeration (`pkg/errors/errors.go`'s own `NewNotFound` comment names the risk explicitly) — that protection was never extended to registration. Compounding side effect: since EH-F-04 validates only the email's RFC 5322 format, not ownership (no confirmation-link step exists), a "successful" enumeration probe (201) also silently registers an account under the probed email, which would then make that email permanently unregisterable by its real owner (DV-F-12 answers 409 forever, no admin/delete path exists). No RU/RD/RNF in the SRS addresses this, and it is not among the 4 accepted risks in SRS §8. Surfaced while writing thesis chapter 4's trust-zone/PKI sections; the user has seen this and, for now, wants it only tracked here — no SRS risk entry, no code change, no GitHub issue yet. | `services/database-vault/internal/registration/registration.go:131-141`, `pkg/errors/errors.go:66-129` (`NewUnauthorized`/`NewConflict`/`NewNotFound` doc comments), `services/entry-hub/internal/httpapi/forward.go:52-66` (`writeForwardedResponse`), `docs/Software_Requirements_Specification.md` DV-F-12/DV-F-15/EH-F-02/EH-F-04/UC-01 |
