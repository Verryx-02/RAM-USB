\chapter{Storage e persistenza}

\section{Database-Vault: cifratura e hashing delle credenziali}
%% TODO: hashing SHA-256 dell'email (DV-F-03), cifratura email con HKDF-SHA256+AES-256-GCM (DV-F-04), hashing password Argon2id salted+peppered (DV-F-06/07), gestione master key.

\section{Database-Vault: registrazione e login}
%% TODO: transazione atomica e creazione utente POSIX con rollback (DV-F-08..12); verifica login e prevenzione oracoli di enumerazione, stesso 401 per email inesistente e password errata (DV-F-15).

\section{Storage-Service: modello di accesso SFTP-only}
%% TODO: accesso esclusivo via SFTP con chiave registrata (ST-F-03), contenuto già cifrato lato client (ST-F-02), divieto di ogni altra forma di SSH (ST-F-04).

\section{Storage-Service: isolamento POSIX e chroot dinamico}
%% TODO: utente POSIX senza home/shell (ST-F-05..09), lookup dinamico della chiave via AuthorizedKeysCommand con fail-secure su qualunque errore (ST-F-11).
